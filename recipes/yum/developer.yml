type: rpm-ostree
install:
  # Apps
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-compose-plugin
  - docker-buildx-plugin

  # Utilities
  - dbus-x11
  - git-credential-libsecret
  - git-lfs
  - pipx
  - uv
  - direnv
  - zoxide
